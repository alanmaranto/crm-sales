{
  "name": "crm-pipeline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sales-db": "json-server --watch src/db/sales-db.json --port 3000",
    "goverment-db": "json-server --watch src/db/goverment-db.json --port 3001",
    "start": "concurrently \"npm run dev\" \"npm run sales-db\" \"npm run goverment-db\""
  },
  "dependencies": {
    "json-server": "^0.17.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-toast-notifications": "^2.5.1",
    "sass": "^1.57.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react": "^3.0.0",
    "concurrently": "^7.6.0",
    "vite": "^4.0.0"
  }
}
